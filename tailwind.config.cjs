const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        'sans': ['Camphor Pro', 'sans-serif']
      }
    },
  },

  plugins: [],
};

module.exports = config;
